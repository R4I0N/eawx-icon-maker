<UserControl x:Class="IconMakinator.Views.AlphanumericPresetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IconMakinator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UniformGrid Margin="5,25,5,10" Width="500" Rows="4" Columns="4" HorizontalAlignment="Center" VerticalAlignment="Center">
        <StackPanel Orientation="Vertical">
            <Image Source="pack://application:,,,/UI/Alphanumeric/units_-_v.1.png" Width="25" Height="25"/>
            <CheckBox Content="One" IsChecked="{Binding RomanOne}" Foreground="{DynamicResource TextForeground}" Margin="5" />
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <Image Source="pack://application:,,,/UI/Alphanumeric/units_-_v.2.png" Width="25" Height="25"/>
            <CheckBox Content="Two" IsChecked="{Binding RomanTwo}" Foreground="{DynamicResource TextForeground}" Margin="5" />
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <Image Source="pack://application:,,,/UI/Alphanumeric/units_-_v.3.png" Width="25" Height="25"/>
            <CheckBox Content="Three" IsChecked="{Binding RomanThree}" Foreground="{DynamicResource TextForeground}" Margin="5" />
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <Image Source="pack://application:,,,/UI/Alphanumeric/units_-_v.4.png" Width="25" Height="25"/>
            <CheckBox Content="Four" IsChecked="{Binding RomanFour}" Foreground="{DynamicResource TextForeground}" Margin="5" />
        </StackPanel>
    </UniformGrid>
</UserControl>
